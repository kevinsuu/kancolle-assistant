# 關卡推薦配裝：本地驗收

本文件對應 `fix/flexible-stage-loadouts`。驗收的是推薦結果與 KC3 完整配裝計算；不需要實際出擊。

## 啟動與共同檢查

1. 在儲存庫根目錄執行 `yarn start`，開啟 KC3 戰略室的「關卡推薦」。
2. 按「重新同步」，再選海域及下表的攻略配置，產生推薦。
3. 裝備與艦娘名稱依 KC3 語系顯示；攻略名稱可能翻譯，括號中的 route ID 可協助對照日誌。
4. 每一案確認：使用持有艦與裝備、同一件裝備沒有重複、裝備可裝、制空／索敵／速度／先制對潛等硬條件通過。
5. 改動遊戲中的編成、裝備或庫存後，先重新同步再測。同一快照的相同請求會使用快取。

推薦會讀取**整個帳號**的裝備，包含其他艦娘身上的裝備。單純卸裝、換編隊不會限制可用庫存。
目前沒有用來指定必選艦或排除裝備的驗收控制項；下表艦艇是準備／比對範例，實際推薦可能選用帳號內更合適的艦艇。
低庫存、特殊競爭與失敗案例可用最後的自動測例驗證，不需要為了驗收廢棄裝備。

## 優先驗收關卡

| 海域與攻略配置                                                 | 準備／比對配置                                                                                                                               | 預期結果                                                                                                                                                                                                                          |
| -------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **3-3：KCWiki・空母軽母** (`3-3-guide-cv-cvl-ca2-dd2`)         | 正規／裝甲空母 1、輕空母 1、巡洋艦系 2、驅逐 2；例如赤城改、飛鷹改、北上改二、大井改二及兩艘驅逐。準備艦戰、艦攻／艦爆、甲標的與主砲／魚雷。 | 硬制空 **160**。不再限定每艘空母恰好兩格攻擊機；可依搭載數把大格改配艦戰，或讓另一空母多帶攻擊機。優先比較保留攻擊機的配法；沒有硬性攻擊任務的空母，也可比較制空箱方案。制空達標後，增加攻擊能力仍可被比較。                      |
| **4-4：攻略網・戦艦空母** (`4-4-guide-bb-cv2-ca-dd-de`)        | 戰艦系 1、正規／裝甲空母 2、重巡 1、驅逐 1、海防 1。選擇帳號內能用一件聲納達先制對潛的驅逐／海防作為比對對象，並準備水面戰裝備。             | 硬制空 **80**、先制對潛至少 **1 艘**。會比較由驅逐或海防負責，以及少量對潛裝備加主砲的配法；不因路線有先制對潛就一律把兩艘都塞滿對潛裝備。水面主力的日戰／夜戰威力仍參與 KC3 排序。天然先制對潛可能讓最終人數高於 1，這不算失敗。 |
| **7-1：攻略網・先制対潜水雷** (`7-1-guide-cl-dd4`)             | 輕巡 1、驅逐 4；準備聲納與爆雷。可用高對潛等級艦、天然先制對潛艦，與仍需兩三格才能達標的艦艇比較。                                           | **5 艘全部**必須通過 KC3 完整配裝的先制對潛檢查。搜尋可保留不同對潛裝備數、雙聲納／爆雷組合與剩餘格主砲方案；不是要求每艘最後都只帶一件聲納。任何一艘未通過時，不能顯示成合法五先制方案。                                         |
| **3-3：KCWiki・空母戦艦** (`3-3-kcwiki-cv-bb-cruiser-dd2`)     | 正規／裝甲空母 1、戰艦系 1、巡洋艦系 2、驅逐 2；巡洋艦可用北上／大井改二。庫存同時有甲標的、可裝主砲兩門、魚雷兩枚。                         | 硬制空 **78**，建議 **108**。雷巡的「甲標的＋雙主砲」與「甲標的＋雙魚雷」都能進入候選；驅逐亦能比較雙主砲／雙魚雷。UI 只保留精算後勝出的同艦隊配装，因此不要求畫面同時顯示兩套，也不預設哪一套一定勝出。                          |
| **2-5：萌新-上路航戰流** (`2-5-north`)                         | 航戰 3＋航巡 2＋重巡 1，或符合攻略艦種限制的輕巡替代。保留低速艦；準備運輸桶至少 2 個、主砲、水偵／瑞雲與索敵裝備。                          | **低速艦隊、制空至少 42、Cn1 索敵至少 49**，兩個桶必須在**不同的兩艘可裝艦**上。搜尋會比較承擔艦，並偏好保留航戰雙主砲與兩格水上機；不能因另一艘改帶桶，就掉到索敵線以下。                                                        |
| **4-5：高速＋戦艦1空母3** (`4-5-fast-plus-battleship-carrier`) | 戰艦系 1、空母系 3、雷巡 1、航巡 1；準備各艦可成立高速＋的渦輪／鍋爐、至少一枚三式彈，以及 KC3 判定可對陸的艦載攻擊機。                      | **制空至少 220、全艦高速＋**，三艘空母都保留對陸攻擊能力；三式彈可由戰艦或航巡承擔。換承擔艦不能覆蓋必要提速裝備、雷巡甲標的或其他必要裝備。具體鍋爐數及改修需求以 KC3 該艦規則為準。                                             |
| **4-4：攻略網・戦艦空母**，瑞雲補充檢查                        | 使用伊勢改二／日向改二，準備兩架相容瑞雲、雙主砲與徹甲彈；也可用只有一架可用瑞雲的測試快照比對。                                             | 瑞雲立體攻擊仍是偏好方案；普通水偵方案也能保留給精算。瑞雲不足或與其他需求衝突時，不應僅因偏好無法成立就把整隊判為無解。                                                                                                          |
| **1-1：攻略網・駆逐4** (`1-1-guide-dd4`)                       | 驅逐 4；低庫存測試快照中，每艘三格，但全帳號只有 8 件相容主砲。                                                                              | 應可顯示留空的一般格與既有空槽警告，而非僅因裝不滿 12 格就無解。一般帳號庫存充裕時，仍優先使用完整配裝，不刻意製造空格。                                                                                                          |

另可用 **1-5「海防艦最軽量」**（`1-5-boss-de`，海防 4 艘、先制對潛至少 4 艘）確認純對潛攻略仍按對潛威力排序，不因加入混合路線評估而改為追求水面火力。

## 規劃耗時驗收

若慢在首次同步，先依 [共用流程與 4-3 驗收](./recommendation-performance.md) 分辨同步、搜尋和 KC3 精算。

1. 更新後重啟 `yarn start`，重新同步帳號，選 **4-4 → 戦艦空母 | ぜかまし**
   （`4-4-guide-bb-cv2-ca-dd-de`），產生一次推薦。按上表檢查戰艦 1、空母 2、重巡 1、
   驅逐 1、海防 1，以及制空至少 80、先制對潛至少 1 艘。
2. 在 `recommendation.completed` 比較 `solverElapsedMs` 與 `exactCombatElapsedMs`。
   前者是本次優化的搜尋部分，後者是 KC3 精算；畫面等待時間還包含通訊與呈現。
   同時記錄 `evaluatedFleetCandidateCount`，目前編成參與比較時可能會多搜尋幾隊。
3. 再次計時前重新同步，讓相同請求快取失效；直接再按產生可能只量到快取。
   使用相同快照、關卡、攻略配置與目標比較，建議記錄三次。
4. 再測 **3-3 空母軽母**（不同搭載格的制空分配）、**4-5 高速＋戦艦1空母3**
   （提速／對陸預留格），以及 **7-1 先制対潜水雷**（無空母）。配装須繼續通過上表硬條件，
   且不能重複使用同一件裝備。

本機 4-4 核心搜尋單次對照（同一機器、18 套精算候選上限）：

| 假想庫存                      | 修改前     | 修改後    |
| ----------------------------- | ---------- | --------- |
| 6 艘／30 件，日向改二瑞雲配置 | 約 3.5 秒  | 約 0.6 秒 |
| 317 艘／1,439 件，大庫存配置  | 約 13.4 秒 | 約 1.3 秒 |

兩組的 **18 套推薦完整內容及順序均與修改前一致**，沒有縮小搜尋寬度或候選上限。
大庫存由既有全海域測試庫存擴充而成，僅數量對齊截圖，並非實際帳號；表中不含 KC3 精算、
同步及 UI 耗時，也不是所有硬體或庫存的時間保證。

搜尋日誌中的 `expandedStateCount` 表示一般格／增設格排序過的候選數，
`materializedStateCount` 表示入選後實際建立配裝狀態的數量；後者通常明顯較少。
`airPowerEvaluationCount` 與 `airPowerCacheHitCount` 分別是完整配裝制空重算次數與快取命中次數。
無解時也保留這些計數與既有最佳制空、所需制空及原因碼，方便判斷是效能問題或條件不足。

## 精算與失敗日誌

若要確認畫面未顯示的候選，檢查應用程式既有推薦日誌：

- `recommendation.completed` 的 `loadoutSearch`：`planCount`、`failedPlanCount`、`flexibleCarrierFleetCount`、`aswAllocationPlanCount`、`specialAssignmentPlanCount`、`emptyRegularSlotSolutionCount`。
- `recommendation.loadout-rerank-completed`：`candidateCount`、`sameFleetVariantCount`、`eligibleCandidateCount`、`surfaceAndAswCandidateCount`、`outcome`、`reasonCodes`、`elapsedMs`。同隊裝備替代案應在合併前計入 `sameFleetVariantCount`。
- `recommendation.oasw-loadout-validation-completed`：KC3 驗證過的先制對潛人數與最低需求。驗證全敗時應有 `OASW_INSUFFICIENT`，而非成功方案。
- `recommendation.combat-evaluation-failed`：KC3 計算器失敗時記錄原因。需要硬性先制對潛驗證的路線不得回退成未驗證結果。

搜尋仍有候選上限，不能解讀成全庫存窮舉或保證全域最佳解；也不是包含命中率、特殊攻擊發動率與道中損耗的完整戰鬥模擬。

## 可重複的最小測例

先執行 `yarn build:recommendation`，再執行：

```sh
node packages/recommendation-core/test/loadout-flexibility.test.js
```

這些測例建立記憶體中的假想庫存，驗證：

- 相同飛機庫存下，二攻二戰只有制空 75，但三戰一攻達 165，通過 150 門檻。
- 兩艘驅逐共用一件聲納時，分配一艘先制對潛，另一艘保留水面配裝。
- 三格艦只有兩件可用裝備時留空；缺少硬性必需的桶時仍然無解。
- 雷巡雙砲與雙魚雷的完整候選都被保留；裝備實例不重複。
- 三式彈、桶由不同承擔艦配置，包括同艦分別佔不同格。
- 同一艦隊的不同配裝進入精算候選池。

完整自動驗證使用 `yarn test:recommendation` 與 `yarn test:shell`。
